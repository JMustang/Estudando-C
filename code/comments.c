#include <stdio.h>
#include <stdlib.h>

int main() {

  // Created a variable
  int data = 34;

  // This code print the variable data
  printf("Data = %d\n", data);

  // This return only happen if theres no error on this code
  return EXIT_SUCCESS;
}
